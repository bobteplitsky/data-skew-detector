import { LightningElement, wire } from 'lwc';
//import getAccountSkewSettings from '@salesforce/apex/DSD_Settings.getAccountSkewSettings'

export default class DsdHomeContainer extends LightningElement {
	// @wire(getAccountSkewSettings) accountSkewSettings;

	// get skewType() {
	// 	return this.accountSkewSettings.data.MNDSD__DSD_Skew_Type__c;
	// }
}